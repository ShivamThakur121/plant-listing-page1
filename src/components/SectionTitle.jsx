const SectionTitle = ({ title }) => (
  <div className="absolute flex top-[2643px] left-[618px] -translate-x-20 border-[2px] border-white whitespace-nowrap">
    <h2 className="text-white text-[55px] font-semibold">{title}</h2>
  </div>
);
export default SectionTitle;